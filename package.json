{
  "name": "student-training-score-root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=@student-score/web",
    "dev:api": "npm run start:dev --workspace=@student-score/api"
  }
}